name: Publish-Cron-Nightly
on:
  # push:
  workflow_dispatch:
  schedule:
    - cron: '30 11 * * *'

jobs:
  publisher:
    runs-on: ubuntu-latest
    env:
      ___X_CMD_CODESORUCE_REPO: dev
      CONCURRENCY: 15
    steps:
      - name: main
        uses: x-cmd/action@main
        with:
          code: ___x_cmd_httpget_gitx_official modlist || true ; echo "$mirror, $___X_CMD_CODESORUCE_REPO, ${___X_CMD_CODESORUCE_REPO_BRANCH}"
